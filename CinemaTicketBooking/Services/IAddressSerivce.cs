using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace CinemaTicketBooking.Services
{
    public interface IAddressSerivce
    {
        //Task<AddressViewModel> GetAddressById(int id);
        //IEnumerable<AddressViewModel> GetAllAddresses();
        //bool AddAddress(CinemaViewModel cinema);
        //bool EditCinema(CinemaViewModel cinema);
        //Task<bool> DeleteCinema(int id);
    }
}
